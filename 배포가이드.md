# 🌐 웹페이지 배포 가이드

## 📋 목차
1. [로컬 네트워크 배포 (같은 WiFi)](#1-로컬-네트워크-배포)
2. [인터넷 배포 (ngrok)](#2-인터넷-배포-ngrok)
3. [클라우드 배포](#3-클라우드-배포)

---

## 1. 로컬 네트워크 배포 (같은 WiFi)

같은 WiFi에 연결된 사람들이 접속할 수 있도록 설정합니다.

### 1단계: 환경 설정

```bash
# .env 파일 생성 (.env.example 참고)
cp .env.example .env
```

`.env` 파일을 열어서 Hugging Face 토큰 입력:
```
HF_TOKEN=your_actual_token_here
```

### 2단계: 서버 실행

```bash
# 의존성 설치
pip install -r requirements.txt

# 서버 시작
python run_server.py
```

### 3단계: 내 IP 주소 확인

**Mac/Linux:**
```bash
ifconfig | grep "inet " | grep -v 127.0.0.1
```

**Windows:**
```cmd
ipconfig
```

예시 출력: `192.168.0.100`

### 4단계: 다른 사람에게 주소 공유

같은 WiFi에 연결된 사람들에게 다음 주소 공유:
```
http://192.168.0.100:8000
```

---

## 2. 인터넷 배포 (ngrok)

인터넷 어디서든 접속 가능하도록 ngrok 사용

### 1단계: ngrok 설치

[https://ngrok.com/download](https://ngrok.com/download) 에서 다운로드

또는 Mac에서:
```bash
brew install ngrok
```

### 2단계: 서버 실행

```bash
python run_server.py
```

### 3단계: 새 터미널에서 ngrok 실행

```bash
ngrok http 8000
```

### 4단계: 공개 URL 확인

ngrok이 생성한 URL을 확인 (예: `https://abcd1234.ngrok.io`)

이 URL을 다른 사람들과 공유하면 인터넷 어디서든 접속 가능!

**⚠️ 주의사항:**
- 무료 ngrok은 세션이 종료되면 URL이 변경됨
- 공개 URL이므로 누구나 접속 가능 (민감한 정보 입력 주의)

---

## 3. 클라우드 배포

### AWS EC2 / Google Cloud / Azure

#### 기본 단계:

1. **클라우드 인스턴스 생성**
   - Ubuntu 22.04 선택
   - 보안 그룹에서 포트 8000 열기

2. **서버 설정**
```bash
# SSH 접속 후
sudo apt update
sudo apt install python3-pip

# 프로젝트 복사
git clone <your_repo>
cd Ai_web_project

# 의존성 설치
pip3 install -r requirements.txt

# .env 파일 설정
nano .env
```

3. **서버 실행 (백그라운드)**
```bash
# nohup으로 실행
nohup python3 run_server.py > server.log 2>&1 &

# 또는 systemd 서비스로 등록
```

4. **방화벽 설정**
```bash
sudo ufw allow 8000
sudo ufw enable
```

5. **접속**
```
http://<클라우드_인스턴스_공개_IP>:8000
```

### Heroku 배포 (간편)

1. **Procfile 생성**
```
web: uvicorn server:app --host 0.0.0.0 --port $PORT
```

2. **배포**
```bash
heroku create your-app-name
git push heroku main
```

---

## 🔒 보안 권장사항

### 개인 이용 목적이라면:

1. **IP 제한 추가** (server.py 수정)
```python
from fastapi import Request

@app.middleware("http")
async def limit_access(request: Request, call_next):
    allowed_ips = ["192.168.0.100", "192.168.0.101"]  # 허용할 IP
    client_ip = request.client.host
    
    if client_ip not in allowed_ips:
        return JSONResponse(
            status_code=403,
            content={"detail": "접근이 거부되었습니다"}
        )
    
    response = await call_next(request)
    return response
```

2. **환경 변수 보호**
   - `.env` 파일을 절대 공개하지 마세요
   - `.gitignore`에 `.env` 추가

3. **HTTPS 사용**
   - 프로덕션에서는 SSL 인증서 적용
   - Let's Encrypt 무료 사용 가능

---

## 🛠️ 문제 해결

### 포트가 이미 사용 중
```bash
# Mac/Linux
lsof -ti:8000 | xargs kill -9

# Windows
netstat -ano | findstr :8000
taskkill /PID <PID번호> /F
```

### 방화벽 차단
```bash
# Mac
시스템 환경설정 > 보안 및 개인정보보호 > 방화벽 > Python 허용

# Windows
Windows Defender 방화벽 > 인바운드 규칙 > 새 규칙 > 포트 8000 허용
```

### CORS 오류
이미 server.py에 CORS 설정이 되어 있으므로 문제 없음

---

## 📞 추가 도움

- FastAPI 공식 문서: https://fastapi.tiangolo.com
- ngrok 문서: https://ngrok.com/docs
- Hugging Face API: https://huggingface.co/docs/api-inference

---

## 🎯 빠른 시작 요약

**가장 간단한 방법 (같은 WiFi):**
```bash
# 1. 환경 설정
cp .env.example .env
# .env 파일에 HF_TOKEN 입력

# 2. 실행
pip install -r requirements.txt
python run_server.py

# 3. IP 확인
ifconfig | grep "inet "

# 4. 공유
# http://<내_IP>:8000 주소를 친구들에게 공유
```

**인터넷 접속 (가장 쉬운 방법):**
```bash
# 터미널 1
python run_server.py

# 터미널 2
ngrok http 8000
# 생성된 URL 공유
```
